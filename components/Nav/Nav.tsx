import styled, { css } from "styled-components"

export const NavCss = css`
  display: flex;
  flex-wrap: wrap;
  padding-left: 0;
  margin-bottom: 0;
  list-style: none;
`

export const Nav = styled.ul`
  ${NavCss}
`